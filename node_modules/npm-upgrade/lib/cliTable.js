'use strict';

var _extends = require('babel-runtime/helpers/extends')['default'];

exports.__esModule = true;
exports.createTable = createTable;
exports.createSimpleTable = createSimpleTable;
var Table = require('cli-table');

function createTable(rows, opts) {
    var table = new Table(opts);

    if (rows) {
        table.push.apply(table, rows);
    }

    return table;
}

function createSimpleTable(rows, opts) {
    return createTable(rows, _extends({}, opts, {
        chars: {
            'top': '',
            'top-mid': '',
            'top-left': '',
            'top-right': '',
            'bottom': '',
            'bottom-mid': '',
            'bottom-left': '',
            'bottom-right': '',
            'left': '',
            'left-mid': '',
            'mid': '',
            'mid-mid': '',
            'right': '',
            'right-mid': '',
            'middle': ''
        }
    }));
}